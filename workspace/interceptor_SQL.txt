SELECT * FROM TBL_MEMBER;

INSERT INTO TBL_MEMBER(ID, MEMBER_EMAIL, MEMBER_PASSWORD, MEMBER_NAME)
VALUES (SEQ_MEMBER.NEXTVAL, 'hds@test.com', '1234', '홍길동');

commit;

CREATE SEQUENCE SEQ_ALARM;

CREATE TABLE TBL_ALARM(
    ID NUMBER CONSTRAINT PK_ALARM PRIMARY KEY,
    POST_ID NUMBER,
    CONSTRAINT FK_ALRAM_POST FOREIGN KEY(POST_ID)
    REFERENCES TBL_POST(ID)
);

SELECT * FROM TBL_POST;

ALTER TABLE TBL_POST ADD (STATUS VARCHAR2(255) DEFAULT 'UNREAD');

commit ;


CREATE SEQUENCE SEQ_FOLLOW;

CREATE TABLE TBL_FOLLOW(
    ID NUMBER CONSTRAINT PK_FOLLOW PRIMARY KEY,
    FOLLOWER_ID NUMBER,
    FOLLOWING_ID NUMBER,
    CONSTRAINT FK_FOLLOWER_MEMBER FOREIGN KEY(FOLLOWER_ID)
    REFERENCES TBL_MEMBER(ID),
    CONSTRAINT FK_FOLLOWING_MEMBER FOREIGN KEY(FOLLOWING_ID)
    REFERENCES TBL_MEMBER(ID)
);

SELECT * FROM TBL_MEMBER;

INSERT INTO TBL_FOLLOW(ID, FOLLOWER_ID, FOLLOWING_ID)
VALUES (SEQ_FOLLOW.NEXTVAL, 21, 1);

SELECT * FROM TBL_FOLLOW;